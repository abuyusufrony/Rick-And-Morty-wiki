import React from 'react';
import { Outlet } from 'react-router';
import Banner from '../Banner/Banner';

const Root = () => {
    return (
        <div>
            <Banner></Banner>

        </div>
    );
};

export default Root;